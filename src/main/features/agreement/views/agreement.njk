{% extends "template.njk" %}

{% from "components/support/macro.njk" import howToGetSupport %}

{% set title = 'home | Contract a Thing' %}
{# {% set heading = t('Welcome to the Money Claim legal rep beta') %} #}

{% block breadCrumb %}
  {{ CCSBreadcrumbs({
        items: [
          {
            text: "Home",
            href: "/home"
          },
          {
            text: "Contract award service",
            href: "#"
          },
          {
            text: "Choose a commercial agreement",
            href: "#"
          }
        ]
      }) }}

{% endblock %}

{% block content %}
<main class="govuk-main-wrapper govuk-main-wrapper--auto-spacing" id="main-content" role="main">
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
      <h1 class="govuk-heading-xl">
       {{data.line1}}
      </h1>
    </div>
  </div>
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full govuk-grid-column-two-thirds-from-desktop">
      <p class="govuk-body-l">{{data.line2}}</p>
      <div class="ccs-choose-agreement">
        <div class="ccs-page-section">
          <h2 class="govuk-heading-m">{{data.line3}} <span>Agreement number: {{agreement_id}}</span> </h2>
          <p class="govuk-body">{{data.line4}}</p>
          <p class="govuk-body">
            <a href="/agreement?id=RM1043.6" class="govuk-link govuk-link--no-visited-state">{{data.line5}}<span class="visuallyhidden">the Digital Specialists and Programmes </span></a>
          </p>
        </div>
        <dl class="ccs-desc-list">
          {# <dt><a href="#" onclick="return false;" class="ccs-workinprogress">Lot 1: Programmes </a> 
            <div class="ccs-hide">Not available in this prototype, please select lot 2</div>
            <span>Expires: <time>DD month YYYY</time> </span>
          </dt>
          <dd>
            <p class="govuk-body"></p>
            <p class="govuk-body"><a href="#" onclick="return false;" class="govuk-link govuk-link--no-visited-state">xx suppliers <span class="visuallyhidden">for Lot 1: Digital Outcomes</span></a> </p>
          </dd> #}
          {% for item in sortedItems %}
          <dt><a href="/projects/create-or-choose?agreement_id={{agreement_id}}&lotId={{item.number}}&aggNameText={{data.line3}}&aggNameValue={{item.name}}" class=" govuk-link govuk-link--no-visited-state">Lot {{item.number}}: {{item.name}}</a> <span>Expires: <time>{{item.endDate}}</time> </span></dt>          <dd>
            <p class="govuk-body"></p>
            <p class="govuk-body"><a href="#" onclick="return false;" class="govuk-link govuk-link--no-visited-state">{{item.description}} <span class="visuallyhidden">for Lot 2: Digital Outcomes</span></a> </p>
          </dd>
          {% endfor %}
        </dl>
      </div>
      <h2 class="govuk-heading-m">{{data.line6}}</h2>
      </div>
      </div>
    </div>
  </main>
{% endblock %}